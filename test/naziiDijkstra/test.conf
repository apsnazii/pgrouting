#!/usr/bin/perl -w

%main::tests = (
    'any' => {
        'comment' => 'Dijkstra test for any versions.',
        'data' => [ ],
        'tests' => [qw(
            doc-naziiDijkstra
            )],
        'documentation' => [qw(
            doc-naziiDijkstra
            )]
    },

);

1;
